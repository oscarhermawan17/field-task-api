services:
  postgres:
    image: postgres:16-alpine
    container_name: field-task-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-field_task}
      POSTGRES_USER: ${POSTGRES_USER:-field_task}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-field_task_dev_password}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
